#pragma once

// Temporary placeholder values.
// This project will later move to AP-based provisioning, so keep this file as a
// simple local override for now.
#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"
